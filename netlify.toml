[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

# Native Next.js build. On Netlify the official @netlify/plugin-nextjs handles
# routing automatically. On Vercel this file is ignored (Vercel auto-detects Next).
